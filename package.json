{
  "name": "status-convertor",
  "version": "1.0.0",
  "description": "Converts status from excel to json",
  "main": "index.js",
  "scripts": {
    "format": "prettier --write ./output/status.json"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "prettier": "^3.2.5"
  }
}
